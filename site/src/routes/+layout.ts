// SPA mode: the locale is chosen client-side, so render on the client only and
// serve a single fallback index.html (see svelte.config.js adapter-static).
export const ssr = false
export const prerender = false
