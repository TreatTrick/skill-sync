# README 教程图片与卖点展示设计

## 目标

让首次打开仓库的访客在最短时间内理解 Skill Sync 的核心价值，同时保留完整的首次配置教程。

## README 编排

- 用新的“快速了解 Skill Sync”章节替换现有的双语言软件截图区。
- `README.md` 只展示英文截图；`README.zh-CN.md` 只展示中文截图。
- 首屏公开展示第 2、4、6、7 张截图，分别突出 GitHub Vault、最小仓库授权、同步前预览，以及跨设备同步与冲突处理。
- 在“Getting started / 开始使用”章节加入折叠式完整 7 步教程，按截图编号展示每步的操作和结果。
- 每张公开截图配一条结果导向说明和描述性 alt 文本，避免只重复界面文案。

## 图片资产

- 英文教程图片放入 `docs/images/tutorial/en/`。
- 中文教程图片放入 `docs/images/tutorial/zh-CN/`。
- 文件名使用统一的 ASCII 英文命名，并保留 `01` 到 `07` 的步骤顺序。
- 图片内容从 `C:\Users\11541\Downloads` 原样复制，不编辑像素内容。

## 范围与验证

- 保留现有技术原理、隐私安全、构建和贡献内容，不做无关改写。
- 验证 14 张图片均已纳入版本控制，两个 README 的语言与图片目录对应，所有相对链接可解析。
- 文档变更后运行 `npm run format:check`；由于只修改 Markdown 与 PNG，不运行与 UI 源码相关的检查。
